Zsh will look for configuration in your $HOME first.

Create a symlink in $HOME for $HOME/.config/zsh/.zshenv.

In .zshenv ZDOTDIR is specified so Zsh will then look for .zshrc in your
$HOME/.config/zsh
